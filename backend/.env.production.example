# 🚀 Configuration PRODUCTION pour Plesk
# Copiez ce fichier en .env et remplissez les valeurs

# Application
NODE_ENV=production
PORT=3001
FRONTEND_URL=https://votredomaine.com
BACKEND_URL=https://api.votredomaine.com

# Database PostgreSQL (configuré dans Plesk)
DATABASE_URL="postgresql://bettracker_user:VOTRE_MOT_DE_PASSE_DB@localhost:5432/bettracker_db"

# Redis (optionnel - si vous l'avez installé)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT - GÉNÉREZ DES SECRETS SÉCURISÉS !
# Utilisez: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=VOTRE_SECRET_JWT_TRES_LONG_ICI
JWT_REFRESH_SECRET=VOTRE_REFRESH_SECRET_TRES_LONG_ICI
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Admin - Credentials pour l'espace admin
ADMIN_EMAIL=admin@votredomaine.com
ADMIN_PASSWORD=VotreMotDePasseAdmin123!

# SMTP - Configuration email
SMTP_HOST=smtp.votrehebergeur.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@votredomaine.com
SMTP_PASSWORD=votre_mot_de_passe_email

# Stripe (pour les paiements)
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PUBLIC_KEY=pk_live_...

# Storage (chemin sur le serveur Plesk)
STORAGE_PATH=/var/www/vhosts/votredomaine.com/storage

# Rate Limiting (protection anti-spam)
THROTTLE_TTL=60
THROTTLE_LIMIT=10

# OpenAI (optionnel - si vous utilisez l'IA)
OPENAI_API_KEY=sk-...

# n8n (optionnel)
N8N_URL=
N8N_API_KEY=
